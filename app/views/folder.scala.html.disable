@(folderPath: com.faacets.data.Main.Path, children: List[com.faacets.data.Main.Path])

@path(folderPath) {
<ul>
  <li><a href="@(Application.pathToUrl(folderPath.parent))">..</a></li>
  @for(child <- children) {
  <li><a href="@(Application.pathToUrl(child))">@(child.lastKey)</a></li>
  }
</ul>
}